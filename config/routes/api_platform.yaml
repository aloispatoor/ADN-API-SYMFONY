api_platform:
    resource:
        swagger:
            api_keys:
                JWT:
                    name: Authorization
                    type: header
        mapping:
            paths: [ '%kernel.project_dir%/src/Entity' ]
        patch_formats:
            json: [ 'application/merge-patch+json' ]
    type: api_platform
    prefix: /api
